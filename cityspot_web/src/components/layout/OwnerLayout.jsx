import { Compass, LayoutDashboard, LogOut, MapPinned, Menu, PlusCircle, User, X } from "lucide-react";
import { useState } from "react";
import { NavLink, Outlet, useNavigate } from "react-router-dom";
import { clearSession, getCurrentUser } from "../../services/sessionStorage";
const links=[{to:"/owner",label:"Resumen",icon:LayoutDashboard,end:true},{to:"/owner/activities",label:"Mis actividades",icon:MapPinned},{to:"/owner/activities/new",label:"Nueva actividad",icon:PlusCircle},{to:"/profile",label:"Mi perfil",icon:User}];
function OwnerLayout(){const [open,setOpen]=useState(false);const nav=useNavigate();const user=getCurrentUser();return <div className="min-h-screen bg-slate-50 lg:flex"><button className="fixed left-4 top-4 z-40 rounded-xl bg-white p-2 shadow lg:hidden" onClick={()=>setOpen(true)}><Menu/></button><aside className={`fixed inset-y-0 left-0 z-50 w-72 border-r bg-white transition-transform lg:static lg:translate-x-0 ${open?"translate-x-0":"-translate-x-full"}`}><div className="flex h-full flex-col"><div className="flex items-center justify-between border-b p-5"><div className="flex items-center gap-2 text-xl font-black"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand-600 text-white"><Compass/></span>CitySpot</div><button className="lg:hidden" onClick={()=>setOpen(false)}><X/></button></div><div className="px-5 py-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">Panel propietario</p><p className="mt-1 truncate font-semibold">{user?.name}</p></div><nav className="flex-1 space-y-1 px-3">{links.map(l=>{const Icon=l.icon;return <NavLink key={l.to} end={l.end} to={l.to} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold ${isActive?"bg-brand-50 text-brand-700":"text-slate-600 hover:bg-slate-100"}`}><Icon size={18}/>{l.label}</NavLink>})}</nav><div className="border-t p-4"><button onClick={()=>{clearSession();nav("/login")}} className="flex w-full items-center justify-center gap-2 rounded-xl border py-2.5 text-sm font-semibold"><LogOut size={17}/>Cerrar sesión</button></div></div></aside><main className="min-w-0 flex-1 px-4 py-16 sm:px-6 lg:px-8 lg:py-8"><Outlet/></main></div>}
export default OwnerLayout;
