import { Compass, Eye, EyeOff, LogIn } from "lucide-react";
import { useState } from "react";
import { Link, useLocation, useNavigate } from "react-router-dom";
import { login } from "../services/authService";
import { saveSession } from "../../../services/sessionStorage";
function LoginPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [show,setShow]=useState(false);const [error,setError]=useState("");const [loading,setLoading]=useState(false);const nav=useNavigate();const location=useLocation();const submit=async e=>{e.preventDefault();setLoading(true);setError("");try{const user=await login(email,password);nav(user.role==="ADMINISTRADOR"?"/admin":user.role==="PROPIETARIO"?"/owner":"/");}catch(err){const registered=JSON.parse(localStorage.getItem("cityspot_registered_user")||"null");if((registered&&registered.email===email&&registered.password===password)||password==="demo123"){const role=registered?.role||(email.includes("admin")?"ADMINISTRADOR":email.includes("propietario")?"PROPIETARIO":"USUARIO");const user={id:1,name:registered?.name||"Usuario Demo",email,role};saveSession({token:"frontend-demo-token",user});nav(role==="ADMINISTRADOR"?"/admin":role==="PROPIETARIO"?"/owner":"/");}else setError("No fue posible iniciar sesión. Para la demo usa cualquier correo y la contraseña demo123.");}finally{setLoading(false)}};return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-slate-100 to-brand-50 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border bg-white p-8 shadow-xl"><Link to="/" className="flex items-center gap-2 text-xl font-black"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand-600 text-white"><Compass/></span>CitySpot</Link><h1 className="mt-8 text-3xl font-black">Bienvenido</h1><p className="mt-2 text-sm text-slate-500">Ingresa para continuar.</p>{location.state?.message&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{location.state.message}</p>}{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="mt-6 block text-sm font-semibold">Correo<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="field mt-1.5" placeholder="usuario@correo.com"/></label><label className="mt-4 block text-sm font-semibold">Contraseña<div className="relative mt-1.5"><input type={show?"text":"password"} required value={password} onChange={e=>setPassword(e.target.value)} className="field pr-10"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><button disabled={loading} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-brand-600 py-3 font-bold text-white disabled:opacity-60"><LogIn size={18}/>{loading?"Ingresando...":"Iniciar sesión"}</button><p className="mt-4 rounded-xl bg-slate-50 p-3 text-xs text-slate-500">Demo frontend: contraseña <strong>demo123</strong>. Usa un correo que contenga “admin” o “propietario” para probar esos roles.</p><p className="mt-5 text-center text-sm text-slate-500">¿No tienes cuenta? <Link to="/register" className="font-semibold text-brand-700">Regístrate</Link></p></form></main>}
export default LoginPage;
