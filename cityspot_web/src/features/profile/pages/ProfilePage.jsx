import { LogOut, Save, UserCircle } from "lucide-react";
import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { clearSession, getCurrentUser, saveSession } from "../../../services/sessionStorage";
function ProfilePage(){const nav=useNavigate();const user=getCurrentUser()||{};const [form,setForm]=useState({name:user.name||"",email:user.email||"",phone:user.phone||""});const [ok,setOk]=useState(false);const save=e=>{e.preventDefault();saveSession({token:localStorage.getItem("cityspot_token"),user:{...user,...form}});setOk(true)};return <main className="mx-auto max-w-4xl px-4 py-12"><div className="rounded-3xl border bg-white p-8 shadow-sm"><div className="flex items-center gap-4"><UserCircle size={56} className="text-brand-600"/><div><h1 className="text-3xl font-black">Mi perfil</h1><p className="text-sm text-slate-500">Rol: {user.role||"USUARIO"} · Estado: ACTIVO</p></div></div>{ok&&<p className="mt-6 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">Datos actualizados correctamente.</p>}<form onSubmit={save} className="mt-8 grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Nombre<input className="field mt-1" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="text-sm font-semibold">Teléfono<input className="field mt-1" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label className="text-sm font-semibold sm:col-span-2">Correo<input type="email" className="field mt-1" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><button className="flex items-center justify-center gap-2 rounded-xl bg-brand-600 py-3 font-bold text-white"><Save size={18}/>Guardar cambios</button><button type="button" onClick={()=>{clearSession();nav("/login")}} className="flex items-center justify-center gap-2 rounded-xl border py-3 font-bold text-slate-700"><LogOut size={18}/>Cerrar sesión</button></form></div></main>}
export default ProfilePage;
