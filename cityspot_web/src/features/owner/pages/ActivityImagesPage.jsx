import { ImagePlus, Star, Trash2 } from "lucide-react";
import { useState } from "react";
import { useParams } from "react-router-dom";
import { activities } from "../../../data/mockData";
function ActivityImagesPage(){const {id}=useParams();const a=activities.find(x=>x.id===Number(id));const [images,setImages]=useState(a?[{id:1,url:a.image,description:"Imagen principal",main:true}]:[]);const upload=e=>{const file=e.target.files?.[0];if(!file)return;if(!["image/jpeg","image/png","image/webp"].includes(file.type))return alert("Formato no permitido");setImages([...images,{id:Date.now(),url:URL.createObjectURL(file),description:file.name,main:images.length===0}])};return <section><h1 className="text-3xl font-black">Imágenes de actividad</h1><p className="mt-2 text-slate-500">Sube, elimina o marca una imagen como principal.</p><label className="mt-6 flex cursor-pointer items-center justify-center gap-2 rounded-2xl border-2 border-dashed border-brand-300 bg-brand-50 p-8 font-bold text-brand-700"><ImagePlus/>Seleccionar imagen<input type="file" accept="image/jpeg,image/png,image/webp" onChange={upload} className="hidden"/></label><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{images.map(img=><article key={img.id} className="overflow-hidden rounded-2xl border bg-white"><img src={img.url} alt={img.description} className="h-48 w-full object-cover"/><div className="p-4"><input value={img.description} onChange={e=>setImages(images.map(x=>x.id===img.id?{...x,description:e.target.value}:x))} className="field"/><div className="mt-3 flex gap-2"><button onClick={()=>setImages(images.map(x=>({...x,main:x.id===img.id})))} className={`flex flex-1 items-center justify-center gap-2 rounded-xl border px-3 py-2 text-sm font-semibold ${img.main?"bg-amber-50 text-amber-700":""}`}><Star size={16}/>{img.main?"Principal":"Hacer principal"}</button><button onClick={()=>setImages(images.filter(x=>x.id!==img.id))} className="rounded-xl border border-red-200 p-2 text-red-600"><Trash2 size={18}/></button></div></div></article>)}</div></section>}
export default ActivityImagesPage;
