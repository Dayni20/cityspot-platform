import { ArrowLeft, Save } from "lucide-react";
import { useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { activities, categories } from "../../../data/mockData";
const empty={categoryId:"",name:"",description:"",city:"",address:"",latitude:"",longitude:"",referencePrice:"",schedule:"",phone:"",email:""};
function ActivityFormPage(){const {id}=useParams();const nav=useNavigate();const existing=activities.find(a=>a.id===Number(id));const [form,setForm]=useState(existing?{...existing}:empty);const [error,setError]=useState("");const change=e=>setForm({...form,[e.target.name]:e.target.value});const submit=e=>{e.preventDefault();if(Number(form.latitude)<-90||Number(form.latitude)>90)return setError("La latitud debe estar entre -90 y 90.");if(Number(form.longitude)<-180||Number(form.longitude)>180)return setError("La longitud debe estar entre -180 y 180.");localStorage.setItem("cityspot_activity_draft",JSON.stringify(form));nav("/owner/activities")};return <section><Link to="/owner/activities" className="flex items-center gap-2 text-sm font-semibold text-slate-600"><ArrowLeft size={17}/>Volver</Link><h1 className="mt-4 text-3xl font-black">{existing?"Editar actividad":"Nueva actividad"}</h1><p className="mt-2 text-slate-500">Completa la información.</p>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<form onSubmit={submit} className="mt-8 grid gap-5 rounded-2xl border bg-white p-6 shadow-sm sm:grid-cols-2"><Field label="Nombre"><input required name="name" value={form.name} onChange={change} className="field"/></Field><Field label="Categoría"><select required name="categoryId" value={form.categoryId} onChange={change} className="field"><option value="">Seleccionar</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></Field><Field label="Descripción" wide><textarea required name="description" value={form.description} onChange={change} className="field min-h-28"/></Field><Field label="Ciudad"><input required name="city" value={form.city} onChange={change} className="field"/></Field><Field label="Dirección"><input required name="address" value={form.address} onChange={change} className="field"/></Field><Field label="Latitud"><input type="number" step="any" name="latitude" value={form.latitude} onChange={change} className="field"/></Field><Field label="Longitud"><input type="number" step="any" name="longitude" value={form.longitude} onChange={change} className="field"/></Field><Field label="Precio referencial"><input type="number" min="0" name="referencePrice" value={form.referencePrice} onChange={change} className="field"/></Field><Field label="Horario"><input name="schedule" value={form.schedule} onChange={change} className="field"/></Field><Field label="Teléfono"><input name="phone" value={form.phone} onChange={change} className="field"/></Field><Field label="Correo"><input type="email" name="email" value={form.email} onChange={change} className="field"/></Field><button className="flex items-center justify-center gap-2 rounded-xl bg-brand-600 py-3 font-bold text-white sm:col-span-2"><Save size={18}/>Guardar actividad</button></form></section>}
function Field({label,wide,children}){return <label className={`text-sm font-semibold ${wide?"sm:col-span-2":""}`}><span className="mb-1.5 block">{label}</span>{children}</label>}
export default ActivityFormPage;
